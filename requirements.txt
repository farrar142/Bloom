# bloom - Python DI Container Framework

# Core dependencies
pydantic>=2.0

# ASGI server (optional, for running the web server)
uvicorn>=0.30.0

# Development dependencies
pytest>=8.0
pytest-asyncio>=0.23
pyyaml