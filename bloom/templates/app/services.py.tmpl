"""{{app_name}} Services

비즈니스 로직을 처리합니다.
"""

from bloom import Component

from .entities import {{app_name_pascal}}
from .repositories import {{app_name_pascal}}Repository


@Component
class {{app_name_pascal}}Service:
    """{{app_name_pascal}} 서비스"""

    repository: {{app_name_pascal}}Repository

    def get_all(self) -> list[{{app_name_pascal}}]:
        """전체 목록 조회"""
        return self.repository.find_all()

    def get_by_id(self, id: int) -> {{app_name_pascal}} | None:
        """ID로 조회"""
        return self.repository.find_by_id(id)

    def save(self, entity: {{app_name_pascal}}) -> {{app_name_pascal}}:
        """저장"""
        return self.repository.save(entity)

    def delete(self, id: int) -> bool:
        """삭제"""
        return self.repository.delete_by_id(id)
